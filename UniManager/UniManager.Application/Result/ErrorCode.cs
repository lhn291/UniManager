namespace UniManager.Application.Result
{
    public enum ErrorCode
    {
        BadRequest = 400,
        NotFound = 404,
        InternalServerError = 500,
        Conflict = 409,
        Unauthorized = 401
    }
}
